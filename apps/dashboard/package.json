{
  "name": "@esim/dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Dashboard microfrontend — independently deployable user dashboard",
  "scripts": {
    "dev":        "next dev --turbo --port 3001",
    "build":      "next build",
    "start":      "next start --port 3001",
    "lint":       "next lint",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@esim/ui":         "workspace:*",
    "@esim/utils":      "workspace:*",
    "@esim/types":      "workspace:*",
    "@esim/api-client": "workspace:*",
    "next":             "^15.0.0",
    "react":            "^19.0.0",
    "react-dom":        "^19.0.0"
  },
  "devDependencies": {
    "@esim/config":     "workspace:*",
    "typescript":       "^5.3.0"
  }
}
